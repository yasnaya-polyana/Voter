{
  "name": "vote-contract",
  "version": "1.0.0",
  "description": "NEAR voting contract",
  "scripts": {
    "build": "asb --target release",
    "test": "asp --nologo",
    "build:docker": "docker build -f Dockerfile.contract -t vote-contract . && docker run --rm -v ${PWD}:/app vote-contract",
    "clean": "rm -rf ./build && mkdir -p build/release"
  },
  "dependencies": {
    "near-sdk-as": "3.2.3"
  },
  "devDependencies": {
    "@assemblyscript/loader": "0.19.23",
    "as-bignum": "0.2.23",
    "assemblyscript": "0.19.23",
    "eslint": "^9.22.0"
  },
  "engines": {
    "node": ">=12.0.0"
  },
  "main": "as-pect.config.js",
  "keywords": [],
  "author": "",
  "license": "ISC"
}
